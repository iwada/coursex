<form action = "<%= courses_path %>" method = "get" id="search">
  <p>
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Search", :name => nil %>  Or Filter by:
    <%= select_tag "coursefilter", options_for_select([ "Level One Courses", "Level Two Courses", "Level Three Courses" ], "MasterCard") %>
    <%= submit_tag "Filter Results", :name => nil %>
  </p>
</form>