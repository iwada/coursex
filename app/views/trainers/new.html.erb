<script>
    $(function ()
    { $("#example").tooltip();
    });
</script>

<div class="row-fluid" >
  <% if  logged_in? and current_user.admin?  %>
  <%= render 'sidebar' %>
      <% else %>
  <%= render 'newtrainersidebar' %>
      
      <% end %>
  <div class="span9" >
    <div class="hero-unit">
      <%= form_for @trainer   do |f|%>
          <%= render 'form' ,:f => f%>



          <div class="actions">
            <%= f.submit "I'm Done",:class=> "btn btn-primary" %>
            <%= link_to 'Cancel', root_path,:class => "btn" %>
          </div>

      <% end %>
    </div>

  </div>

</div>



